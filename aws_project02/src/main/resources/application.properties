# mvn package -Dspring.profiles.active=prod or dev
spring.profiles.active=prod

server.port=9091

aws.endpoint=http://localhost:4566
aws.region=us-east-1
aws.sqs.queue.product.events.name=product-events